<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.damien.devicerecord">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".homepage" />
        <activity android:name=".device" />
        <activity android:name=".add_list" />
        <activity android:name=".edit_profile" />
        <activity android:name=".profile" />
        <activity android:name=".device_details" />
        <activity android:name=".device_final_details" />
        <activity android:name=".edit_details" />
        <activity android:name=".manage_balance" />
        <activity android:name=".balance_history" />
        <activity android:name=".balance_final_history" />
        <activity android:name=".add_list_next" />

        <receiver android:name=".AlertReceiver" />

        <activity android:name=".add_device"></activity>
    </application>

</manifest>